services:
    sensor-publisher:
        build: .
        container_name: kafka_sensors
        command: python Sensores.py
        # depends_on:
        #   - kafka

    parking-web-ui:
        build: .
        container_name: web_interface
        ports:
        - "5000:5000"
        command: python ParkingWeb.py
        # depends_on:
        #   - mongodb